{% extends 'app_shell.html.twig' %}

{% block title %}Años escolares{% endblock %}

{% block breadcrumb %}<a href="{{ path('app_homepage') }}">Inicio</a><span class="sep">/</span>Años escolares{% endblock %}

{% block body %}
    <div class="page-head">
        <div>
            <h1>Años escolares</h1>
            <p class="lead">Cada año escolar con las fechas de sus tres trimestres. Sirven para calcular los plazos de las tareas (p. ej. «fin del trimestre» o «antes de vacaciones») y para generar las tareas del año con un clic.</p>
        </div>
        <a class="btn" href="{{ path('admin_academic_year_new') }}">+ Nuevo año escolar</a>
    </div>

    <div class="card">
        {% if years is empty %}
            <div class="empty-state">
                <h2>Aún no hay ningún año escolar definido</h2>
                <p>Define un año escolar con sus trimestres para poder fijar plazos relativos al calendario y generar las tareas del año.</p>
                <a class="btn" href="{{ path('admin_academic_year_new') }}">Nuevo año escolar</a>
            </div>
        {% else %}
            <table data-list-tools>
                <thead>
                    <tr><th>Curso</th><th>1.er trimestre</th><th>2.º trimestre</th><th>3.er trimestre</th><th></th></tr>
                </thead>
                <tbody>
                    {% for year in years %}
                        <tr>
                            <td class="mono">{{ year.schoolYear }}</td>
                            <td class="mono">{{ year.term1Start|date('d/m/Y') }} – {{ year.term1End|date('d/m/Y') }}</td>
                            <td class="mono">{{ year.term2Start|date('d/m/Y') }} – {{ year.term2End|date('d/m/Y') }}</td>
                            <td class="mono">{{ year.term3Start|date('d/m/Y') }} – {{ year.term3End|date('d/m/Y') }}</td>
                            <td class="row-actions">
                                <a href="{{ path('admin_academic_year_edit', { id: year.id }) }}">Editar</a>
                                <form method="post" action="{{ path('admin_academic_year_generate', { id: year.id }) }}" data-confirm="¿Generar las tareas del curso {{ year.schoolYear }}? Se crearán las tareas de las plantillas con regla de fecha (no se duplican las que ya existan).">
                                    <input type="hidden" name="_token" value="{{ csrf_token('academic_year_generate' ~ year.id) }}">
                                    <button type="submit" class="link-action">Generar año escolar</button>
                                </form>
                                <form method="post" action="{{ path('admin_academic_year_delete', { id: year.id }) }}" data-confirm="¿Seguro que quieres borrar la estructura del curso {{ year.schoolYear }}? También se borrará el horario importado de ese curso (el parte de guardias se queda sin horario hasta reimportarlo).">
                                    <input type="hidden" name="_token" value="{{ csrf_token('academic_year_delete' ~ year.id) }}">
                                    <button type="submit" class="link-danger">Borrar</button>
                                </form>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% endif %}
    </div>
{% endblock %}
